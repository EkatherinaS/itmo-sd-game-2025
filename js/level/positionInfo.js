export class PositionInfo {
    constructor(damage, isEntry, isExit, blocked) {
        this.damage = damage;
        this.isEntry = isEntry;
        this.isExit = isExit;
        this.blocked = blocked;
    }
}
